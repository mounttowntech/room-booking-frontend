# Room Booking Billing Frontend Static Demo

## Run locally
npm install
npm run dev

## Build for Netlify
npm run build

Upload the `dist` folder to Netlify, or connect this folder to GitHub and set build command `npm run build`, publish directory `dist`.
